name: file_editor_platform_interface
description: A common platform interface for the file_editor plugin.
version: 1.0.0

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.1.4

dev_dependencies:
  lints: ^2.0.0
